server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/adrianrm97/bootcamp.git
          searchPaths: config-data
  security:
    user:
      name: root
      password: toor